// const test = require('tape').test
// const navbar = require('../routes/utils/navbar.js')

// test('generateLinks', (t) => {
// 	const links = navbar.generateLinks(0)
// 	links.forEach((link) => t.equal(link.active, link.id === 0, 'should set the correct link to active'))
// 	t.end()
// })
